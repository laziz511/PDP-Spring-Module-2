<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/util
           https://www.springframework.org/schema/util/spring-util.xsd">

    <!-- City Bean with Prototype Scope -->
    <bean id="city" class="uz.pdp.online.xml_config.City" scope="prototype" p:name="#{cityProperties.name}"
          p:numberOfPeople="#{cityProperties.numberOfPeople}" p:size="#{cityProperties.size}"/>

    <!-- City Properties -->
    <util:properties id="cityProperties" location="classpath:city.properties"/>

</beans>
